import forNative, {
  css as cssNative,
  ThemeProvider as providerNative,
} from 'styled-components/native';

export const styled = forNative;
export const css = cssNative;
export const ThemeProvider = providerNative;
