import React from 'react';
import { storiesOf } from '../../utils/story';
import { WorkingButton } from './WorkingButton';

storiesOf('03 Working button', module).add('Default', () => <WorkingButton />);
