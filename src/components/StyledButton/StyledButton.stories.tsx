import React from 'react';
import { storiesOf } from '../../utils/story';
import { StyledButton } from './StyledButton';

storiesOf('02 Styled button', module).add('Default', () => <StyledButton />);
